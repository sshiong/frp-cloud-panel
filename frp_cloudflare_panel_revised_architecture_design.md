# FRP 多用户云隧道管理平台技术设计方案（双面板架构增强版）

## 1. 架构定位

系统采用两个独立部署组件：

-   Server Panel：部署在 FRPS 服务器，负责全局控制、安全、资源仲裁。
-   Client Panel：用户侧本地服务，负责 Web 面板、本地 FRPC
    管理和状态采集。

两者不是同一个面板，也不增加第三个 Agent。

------------------------------------------------------------------------

# 2. Server Panel

Server Panel 职责：

-   管理员 Web 面板
-   控制 API
-   用户权限管理
-   FRP 用户凭证管理
-   全局端口管理
-   域名资源管理
-   Cloudflare Token 加密保存
-   DNS 管理
-   ACME 证书申请和续期
-   HTTP/HTTPS Router
-   FRPS 鉴权
-   中央数据库
-   数据导入导出
-   审计日志

Server Panel 保持轻量，只负责控制面和全局资源管理。

------------------------------------------------------------------------

# 3. Client Panel

Client Panel 是完整本地服务，不是静态网页。

包含：

-   用户 Web 面板
-   本地 API
-   Server Panel API 客户端
-   FRPC 配置管理
-   FRPC 进程管理
-   本地服务检测
-   状态采集
-   日志管理

用户通过 Client Panel：

-   创建映射
-   修改映射
-   删除映射
-   启停 FRPC
-   修改 frpc.toml
-   重载 FRPC
-   查看日志
-   检查本地端口

------------------------------------------------------------------------

# 4. Client Panel 配置应用流程

禁止直接覆盖配置。

流程：

    获取服务端期望配置
    ↓
    生成临时 frpc.toml
    ↓
    配置检查
    ↓
    备份当前配置
    ↓
    原子替换
    ↓
    FRPC reload
    ↓
    检查代理状态
    ↓
    上报结果

失败：

-   自动恢复旧配置
-   再次 reload
-   状态改为 config_error
-   保存错误日志
-   上报 Server Panel

------------------------------------------------------------------------

# 5. 数据权限原则

Server Panel 是最终事实来源：

管理：

-   用户
-   FRP Token
-   Client ID
-   远程端口
-   域名
-   CF Token状态
-   DNS关系
-   HTTPS模式
-   证书状态
-   配置版本

Client Panel 是本地状态来源：

管理：

-   FRPC运行状态
-   本地配置
-   本地端口健康
-   应用结果
-   本地日志

------------------------------------------------------------------------

# 6. Client 注册认证

首次部署输入：

-   Server 地址
-   用户名
-   密码
-   客户端名称

成功后：

Server 创建：

    client_id
    device_token

Client 保存设备凭证。

以后通信：

    client_id + device_token

服务端只保存：

device_token hash

支持：

-   撤销设备
-   重置设备
-   停用设备
-   查看最后连接时间
-   查看 IP
-   查看版本

------------------------------------------------------------------------

# 7. 通信机制

采用：

    HTTPS REST API + WebSocket

REST：

-   配置管理
-   登录
-   Token上传
-   DNS管理

WebSocket：

-   配置变化通知
-   停用通知
-   Token重置通知
-   实时状态

心跳：

30～60秒。

断线：

指数退避重连。

------------------------------------------------------------------------

# 8. 数据库

默认：

SQLite。

开启：

    WAL模式

原因：

-   单机部署简单
-   并发读取更好
-   无额外数据库依赖

未来支持：

-   PostgreSQL
-   MySQL

------------------------------------------------------------------------

# 9. 映射状态机

状态：

    reserved
    pending_apply
    running
    offline
    config_error
    disabled
    deleting

Server 保存期望状态。

Client 上报实际状态。

------------------------------------------------------------------------

# 10. 端口管理

远程端口：

数据库强约束：

    UNIQUE(server_id, remote_port)

规则：

在线、离线、错误状态均占用。

只有删除映射释放。

支持：

-   手动指定端口
-   自动分配空闲端口

自动分配必须在事务中完成。

------------------------------------------------------------------------

# 11. 映射创建流程

    Client提交
    ↓
    Server事务锁端口
    ↓
    创建资源
    ↓
    生成配置版本
    ↓
    Client应用配置
    ↓
    FRPC reload
    ↓
    上报结果

创建资源成功不等于运行成功。

------------------------------------------------------------------------

# 12. 删除流程

正常：

    标记 deleting
    ↓
    通知Client
    ↓
    删除本地代理
    ↓
    确认
    ↓
    删除数据库
    ↓
    释放端口

客户端离线：

管理员可以强制删除。

Client 下次上线：

必须全量同步。

旧配置禁止重新注册。

------------------------------------------------------------------------

# 13. 配置版本

维护：

    desired_config_version
    applied_config_version

任何变化增加版本：

-   创建映射
-   修改映射
-   删除映射
-   Token重置
-   用户停用

Client上线：

版本不一致：

拉取完整配置。

------------------------------------------------------------------------

# 14. FRP二次鉴权

不能信任 Client Panel。

FRPS 登录：

检查：

-   用户
-   Token
-   Client ID
-   状态

创建代理：

检查：

-   端口归属
-   域名归属
-   映射状态
-   配置版本

停用用户：

拒绝新连接。

------------------------------------------------------------------------

# 15. Cloudflare Token安全

保存位置：

仅 Server Panel。

加密：

AES-256-GCM。

数据库：

    ciphertext
    nonce

禁止：

-   明文返回
-   日志输出
-   明文导出

导出：

必须排除或保持密文。

------------------------------------------------------------------------

# 16. HTTPS架构

统一：

Server Router 终止 TLS。

流程：

    浏览器
    ↓
    Cloudflare(可选)
    ↓
    Server Router
    ↓
    FRPS vhost_http_port
    ↓
    FRPC

------------------------------------------------------------------------

# 17. HTTPS三模式

## 自动证书

-   CF代理关闭
-   Let's Encrypt / ZeroSSL
-   DNS-01验证
-   Server Router加载证书

证书路径：

    /data/certs/domain/

支持热加载。

------------------------------------------------------------------------

## Cloudflare代理

-   开启橙色云
-   Cloudflare边缘HTTPS
-   源站继续HTTPS
-   推荐 Full(strict)
-   推荐 Origin CA

------------------------------------------------------------------------

## 无证书

-   仅HTTP
-   不为该域名配置HTTPS

------------------------------------------------------------------------

# 18. 证书管理

每天检查。

30天内：

自动续期。

手动续期：

60秒冷却。

使用动态：

tls.Config GetCertificate

无需重启。

------------------------------------------------------------------------

# 19. DNS管理

支持：

-   创建A记录
-   修改A记录
-   删除记录
-   一键更新服务器IP

------------------------------------------------------------------------

# 20. Zone匹配

调用：

Cloudflare Zones API。

规则：

-   分页获取
-   小写化
-   IDNA处理
-   最长合法DNS后缀匹配

无权限：

拒绝。

------------------------------------------------------------------------

# 21. Token重置

流程：

3秒红色确认。

执行：

删除：

-   Token密文
-   Nonce
-   Zone缓存

立即失效。

FRP Token重置：

-   旧Token失效
-   增加配置版本
-   通知Client更新

------------------------------------------------------------------------

# 22. 数据安全

密码：

bcrypt。

Session：

JWT。

保护：

-   CSRF
-   SQL注入
-   暴力登录

日志禁止：

-   密码
-   Token
-   私钥

------------------------------------------------------------------------

# 23. 数据备份

JSON导出：

禁止：

-   CF Token明文
-   FRP Token明文

推荐：

加密备份包：

包含：

-   数据快照
-   密文数据
-   证书
-   配置
-   校验信息

使用管理员备份密码加密。

------------------------------------------------------------------------

# 24. 审计日志

记录：

-   登录
-   用户操作
-   Token操作
-   DNS修改
-   证书操作
-   导入导出

不记录：

敏感数据。

------------------------------------------------------------------------

# 25. 开发选型总结

后端：

-   Go
-   Gin
-   GORM

数据库：

-   SQLite WAL

前端：

-   Vue3
-   TypeScript
-   Element Plus

通信：

-   HTTPS REST
-   WebSocket

核心原则：

Client Panel 管理用户机器。

Server Panel 管理全局资源。

FRP负责高速转发。

控制面负责安全和自动化。
